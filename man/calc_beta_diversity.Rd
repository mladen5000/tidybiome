% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/beta_diversity.R
\name{calc_beta_diversity}
\alias{calc_beta_diversity}
\title{Calculate Beta Diversity Dissimilarity and Distance Matrices}
\usage{
calc_beta_diversity(
  tb,
  metric = c("raitchison", "aitchison", "bray", "jaccard", "jsd"),
  assay = "counts",
  pseudocount = 1
)
}
\arguments{
\item{tb}{A \code{tidy_microbiome} object.}

\item{metric}{Character string specifying the distance metric:
\itemize{
\item \code{"raitchison"}: Robust Aitchison distance (Euclidean distance on the \code{rclr} matrix).
Recommended for zero-inflated microbiome data as it avoids pseudocount distortion.
\item \code{"aitchison"}: Classic Aitchison distance (Euclidean distance on pseudocount CLR).
\item \code{"bray"}: Bray-Curtis dissimilarity.
\item \code{"jaccard"}: Jaccard binary dissimilarity.
\item \code{"jsd"}: Jensen-Shannon distance (\eqn{\sqrt{\text{JSD}}}).
}}

\item{assay}{Character string naming the abundance assay to use. Defaults to \code{"counts"}.}

\item{pseudocount}{Numeric pseudocount for \code{"aitchison"}. Defaults to 1.}
}
\value{
An updated \code{tidy_microbiome} object with the computed distance stored in its metadata.
}
\description{
Computes beta diversity distance matrices between samples, including modern
compositionally coherent distances (Robust Aitchison, Jensen-Shannon) and classic
ecological metrics (Bray-Curtis, Jaccard, classic Aitchison). The resulting
distance matrix is stored in the \code{tidy_microbiome} metadata and can be retrieved
using \code{\link[=get_distance]{get_distance()}}.
}
\examples{
counts <- matrix(c(10, 0, 5, 20, 10, 0, 5, 5, 5), nrow = 3, ncol = 3,
                 dimnames = list(c("A", "B", "C"), c("S1", "S2", "S3")))
tb <- tidy_microbiome(counts)
tb <- calc_beta_diversity(tb, metric = "raitchison")
d <- get_distance(tb, "raitchison")
}
