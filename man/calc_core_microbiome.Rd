% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core_microbiome.R
\name{calc_core_microbiome}
\alias{calc_core_microbiome}
\title{Identify Core Microbiome Members}
\usage{
calc_core_microbiome(
  tb,
  method = c("threshold", "inflection"),
  min_prevalence = 0.8,
  min_abundance = 0.001,
  assay = "counts"
)
}
\arguments{
\item{tb}{A \code{tidy_microbiome} object.}

\item{method}{Character string specifying the core detection method:
\itemize{
\item \code{"inflection"}: Data-driven inflection curve analysis. Evaluates the sorted
prevalence distribution across taxa and finds the natural elbow/inflection point.
\item \code{"threshold"}: Static filter on minimum prevalence and relative abundance.
}}

\item{min_prevalence}{Numeric minimum prevalence threshold (between 0 and 1) for \code{"threshold"} method.
Defaults to 0.8 (present in at least 80\% of samples).}

\item{min_abundance}{Numeric minimum relative abundance threshold for a taxon to be considered present.
Defaults to 0.001 (0.1\%).}

\item{assay}{Character string naming the abundance assay. Defaults to \code{"counts"}.}
}
\value{
A \link[tibble:tbl_df]{tibble::tbl_df} with one row per taxon containing \code{taxon_id},
\code{prevalence}, \code{mean_abundance}, \code{is_core}, and taxonomic annotations.
}
\description{
Detects the core microbiome members across samples using either modern data-driven
inflection curve analysis (identifying natural mathematical breakpoints in the
prevalence spectrum) or classic static threshold filtering.
}
\examples{
counts <- matrix(c(50, 40, 60, 55, 45, 0, 0, 10, 0, 0), nrow = 2, byrow = TRUE,
                 dimnames = list(c("CoreTaxon", "RareTaxon"), paste0("S", 1:5)))
tb <- tidy_microbiome(counts)
core_df <- calc_core_microbiome(tb, method = "threshold", min_prevalence = 0.8)
}
