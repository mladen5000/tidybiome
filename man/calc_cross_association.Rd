% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/associations.R
\name{calc_cross_association}
\alias{calc_cross_association}
\title{Calculate Cross-Associations Between Taxa and Metadata Variables}
\usage{
calc_cross_association(
  tb,
  variables = NULL,
  assay = "counts",
  method = c("spearman", "pearson"),
  p_adj_method = "BH",
  sort = TRUE
)
}
\arguments{
\item{tb}{A \code{tidy_microbiome} object.}

\item{variables}{Character vector of numeric metadata columns in \code{tb} to correlate with taxa.
If \code{NULL}, automatically selects all numeric columns in sample metadata. Defaults to \code{NULL}.}

\item{assay}{Name of assay to evaluate. Defaults to \code{"counts"}.}

\item{method}{Correlation method: \code{"spearman"} or \code{"pearson"}. Defaults to \code{"spearman"}.}

\item{p_adj_method}{Multiple testing correction method passed to \code{stats::p.adjust} (e.g. \code{"BH"}, \code{"bonferroni"}).
Defaults to \code{"BH"}.}

\item{sort}{Logical; if \code{TRUE}, sorts output by adjusted p-value (\code{padj}). Defaults to \code{TRUE}.}
}
\value{
A tidy tibble with columns:
\itemize{
\item \code{taxon_id}: Taxon identifier.
\item \code{variable}: Metadata covariate name.
\item \code{correlation}: Estimated correlation coefficient.
\item \code{p_value}: Raw association p-value from \code{cor.test}.
\item \code{padj}: Multiple-testing adjusted p-value.
}
Joined with taxonomic lineages from \code{tax_table}.
}
\description{
Computes pairwise associations (Spearman or Pearson correlations and significance)
between microbial feature abundances and continuous metadata covariates (e.g. host age, BMI, clinical metrics).
}
