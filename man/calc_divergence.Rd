% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/divergence.R
\name{calc_divergence}
\alias{calc_divergence}
\title{Calculate Ecological Divergence Relative to a Reference State}
\usage{
calc_divergence(
  tb,
  reference = "median",
  method = "bray",
  assay = "counts",
  add_to_metadata = TRUE
)
}
\arguments{
\item{tb}{A \code{tidy_microbiome} object.}

\item{reference}{Reference baseline to compare against. Can be:
\itemize{
\item \code{"median"}: Cohort median abundance profile across all samples.
\item \code{"mean"}: Cohort mean abundance profile.
\item A named list specifying a metadata subset (e.g. \code{list(treatment = "Control")}).
\item A character string identifying a specific reference \code{sample_id}.
}}

\item{method}{Dissimilarity metric: \code{"bray"} (Bray-Curtis), \code{"jsd"} (Jensen-Shannon divergence),
or \code{"euclidean"}. Defaults to \code{"bray"}.}

\item{assay}{Assay to evaluate. Defaults to \code{"counts"}.}

\item{add_to_metadata}{Logical; if \code{TRUE}, appends column \code{divergence} to \code{tb}'s sample metadata.
If \code{FALSE}, returns a tidy tibble. Defaults to \code{TRUE}.}
}
\value{
A \code{tidy_microbiome} object with \code{divergence} in metadata (if \code{add_to_metadata = TRUE}),
or a tibble with \code{sample_id} and \code{divergence}.
}
\description{
Evaluates the dissimilarity of each sample's community profile against a reference
baseline (such as a healthy control group, cohort median profile, or designated baseline sample).
}
