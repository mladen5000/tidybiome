% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prevalence.R
\name{calc_prevalence}
\alias{calc_prevalence}
\title{Calculate Prevalence and Abundance Metrics Across Taxa}
\usage{
calc_prevalence(tb, assay = "counts", detection = 0, sort = TRUE)
}
\arguments{
\item{tb}{A \code{tidy_microbiome} object.}

\item{assay}{Assay to evaluate. Defaults to \code{"counts"}.}

\item{detection}{Abundance threshold above which a taxon is considered detected. Defaults to \code{0}.}

\item{sort}{Logical; if \code{TRUE}, sorts taxa in descending order of prevalence. Defaults to \code{TRUE}.}
}
\value{
A tibble with columns:
\itemize{
\item \code{taxon_id}: Unique identifier for each taxon.
\item \code{prevalence}: Proportion of samples where taxon was detected (\eqn{[0, 1]}).
\item \code{prevalence_n}: Count of samples where taxon was detected.
\item \code{mean_abundance}: Average abundance across all samples.
\item \code{median_abundance}: Median abundance across all samples.
}
Joined with available taxonomic ranks from \code{tax_table}.
}
\description{
Evaluates the proportion and number of samples where each taxon exceeds a specified
detection threshold, along with mean and median abundances.
}
