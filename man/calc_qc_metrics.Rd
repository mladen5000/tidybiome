% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc.R
\name{calc_qc_metrics}
\alias{calc_qc_metrics}
\title{Calculate Sample-Level Quality Control (QC) Metrics}
\usage{
calc_qc_metrics(tb, assay = "counts", augment = TRUE)
}
\arguments{
\item{tb}{A \code{tidy_microbiome} object.}

\item{assay}{Name of the assay to compute metrics on (defaults to \code{"counts"}).}

\item{augment}{Logical; if \code{TRUE} (default), the QC metrics are appended directly as columns
to the sample metadata within the returned \code{tidy_microbiome} object, enabling seamless
filtering in dplyr pipelines. If \code{FALSE}, returns a tidy \link[tibble:tibble]{tibble::tibble} of QC metrics.}
}
\value{
If \code{augment = TRUE}, an updated \code{tidy_microbiome} object with added columns:
\describe{
\item{qc_total_reads}{Total sum of read counts in the sample.}
\item{qc_n_features}{Number of features (taxa) observed with count > 0.}
\item{qc_sparsity}{Proportion of unobserved features in the sample (fraction of zeros).}
\item{qc_top_taxon_share}{Proportion of total sample library represented by the single most abundant taxon.}
\item{qc_shannon}{Shannon diversity / entropy index calculated from sample proportions.}
}
If \code{augment = FALSE}, returns a tibble with \code{sample_id} and the above columns.
}
\description{
Computes key quality control and diagnostic statistics for each sample in a
\code{tidy_microbiome} object, including total library size, observed feature richness,
sample-level sparsity, dominance of the top taxon, and Shannon entropy.
}
\examples{
data(gut_microbiome)
# Augment metadata with QC metrics and filter high-quality samples
tb_clean <- calc_qc_metrics(gut_microbiome)
tb_filtered <- tb_clean[tb_clean$qc_total_reads >= 5000, ]

# Or extract QC metrics as a tibble
qc_df <- calc_qc_metrics(gut_microbiome, augment = FALSE)
}
