% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_alpha}
\alias{plot_alpha}
\title{Plot Alpha Diversity with Statistical Comparisons}
\usage{
plot_alpha(
  tb,
  metric = "hill_1",
  x = NULL,
  color = NULL,
  test = TRUE,
  palette = "tidybiome"
)
}
\arguments{
\item{tb}{A \code{tidy_microbiome} object.}

\item{metric}{Character string naming the alpha diversity metric (e.g. \code{"hill_1"}, \code{"hill_0"}, \code{"shannon"}).}

\item{x}{Character string naming the metadata column for the horizontal grouping axis.}

\item{color}{Optional character string naming the metadata column to color by.}

\item{test}{Logical. If \code{TRUE} and \code{x} has 2 groups, runs a Wilcoxon rank-sum test and displays the p-value.}

\item{palette}{Palette name: \code{"tidybiome"} or \code{"nature"}.}
}
\value{
A \code{ggplot2::ggplot} object.
}
\description{
Generates publication-grade hybrid raincloud/boxplot visuals for alpha diversity metrics
(including Hill numbers \eqn{q = 0, 1, 2}), with overlaid sample points and automated
two-group statistical significance annotations.
}
\examples{
counts <- matrix(c(25, 25, 25, 25, 10, 5, 2, 80), nrow = 4, ncol = 2,
                 dimnames = list(paste0("T", 1:4), c("S1", "S2")))
sample_data <- data.frame(sample_id = c("S1", "S2"), treatment = c("Control", "Treated"))
tb <- tidy_microbiome(counts, sample_data)
tb <- calc_alpha_diversity(tb, metrics = "hill")
p <- plot_alpha(tb, metric = "hill_1", x = "treatment")
}
