% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_composition}
\alias{plot_composition}
\title{Plot Taxonomic Composition with Intelligent Top-N Grouping}
\usage{
plot_composition(
  tb,
  rank = "Genus",
  top_n = 8,
  group_by = NULL,
  palette = "tidybiome"
)
}
\arguments{
\item{tb}{A \code{tidy_microbiome} object.}

\item{rank}{Character string specifying taxonomic rank (e.g. \code{"Genus"}, \code{"Phylum"}).
If \code{NULL}, uses ASV/feature IDs.}

\item{top_n}{Number of top most abundant taxa to display individually (default: 8).}

\item{group_by}{Optional character string specifying a sample metadata column to facet by.}

\item{palette}{Palette name: \code{"tidybiome"} or \code{"nature"}.}
}
\value{
A \code{ggplot2::ggplot} object.
}
\description{
Generates publication-ready stacked bar charts of taxonomic composition.
Automatically aggregates less abundant taxa into an aesthetically styled \code{"Other"}
category at the base of the plot, eliminating cluttered legends.
}
\examples{
counts <- matrix(c(100, 20, 5, 2, 5, 10, 80, 70), nrow = 2, byrow = TRUE,
                 dimnames = list(c("Tax1", "Tax2"), c("S1", "S2", "S3", "S4")))
sample_data <- data.frame(sample_id = c("S1", "S2", "S3", "S4"), group = c("A", "A", "B", "B"))
tax_table <- data.frame(taxon_id = c("Tax1", "Tax2"), Genus = c("Bacteroides", "Prevotella"))
tb <- tidy_microbiome(counts, sample_data, tax_table)
p <- plot_composition(tb, rank = "Genus", top_n = 2)
}
