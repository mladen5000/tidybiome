% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_heatmap}
\alias{plot_heatmap}
\title{Plot Compositional Microbiome Heatmap}
\usage{
plot_heatmap(
  tb,
  rank = NULL,
  top_n = 25,
  assay = "counts",
  scale = c("log10", "relabundance", "rclr", "none"),
  cluster_samples = TRUE,
  cluster_taxa = TRUE,
  annotation_col = NULL,
  palette = "viridis"
)
}
\arguments{
\item{tb}{A \code{tidy_microbiome} object.}

\item{rank}{Optional taxonomic rank to aggregate by before plotting (e.g. \code{"Genus"}).}

\item{top_n}{Number of top abundant taxa to display (default: 25).}

\item{assay}{Assay to extract and plot. Defaults to \code{"counts"}.}

\item{scale}{Scaling applied to values: \code{"log10"}, \code{"relabundance"}, \code{"rclr"}, or \code{"none"}.}

\item{cluster_samples}{Logical. If \code{TRUE} (default), clusters samples via hierarchical clustering.}

\item{cluster_taxa}{Logical. If \code{TRUE} (default), clusters taxa via hierarchical clustering.}

\item{annotation_col}{Optional sample metadata column to group or facet samples by.}

\item{palette}{Palette option: \code{"viridis"}, \code{"magma"}, or \code{"plasma"}.}
}
\value{
A \code{ggplot2::ggplot} object.
}
\description{
Generates an aesthetic, publication-ready abundance heatmap for top taxa across samples,
featuring hierarchical clustering of samples and taxa, compositional scaling,
and optional metadata grouping.
}
\examples{
counts <- matrix(c(100, 20, 5, 2, 5, 10, 80, 70), nrow = 2, byrow = TRUE,
                 dimnames = list(c("Tax1", "Tax2"), c("S1", "S2", "S3", "S4")))
sample_data <- data.frame(sample_id = c("S1", "S2", "S3", "S4"), group = c("A", "A", "B", "B"))
tax_table <- data.frame(taxon_id = c("Tax1", "Tax2"), Genus = c("Bacteroides", "Prevotella"))
tb <- tidy_microbiome(counts, sample_data, tax_table)
p <- plot_heatmap(tb, top_n = 2)
}
