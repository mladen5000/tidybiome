% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_ordination}
\alias{plot_ordination}
\title{Plot Microbiome Ordination and Compositional Biplots}
\usage{
plot_ordination(
  tb,
  method = c("rpca", "pcoa", "pca"),
  color = NULL,
  shape = NULL,
  ellipse = TRUE,
  biplot = FALSE,
  top_taxa = 5,
  palette = "tidybiome"
)
}
\arguments{
\item{tb}{A \code{tidy_microbiome} object. Run \code{\link[=calc_ordination]{calc_ordination()}} beforehand or specify \code{method}.}

\item{method}{Character string specifying ordination method (\code{"rpca"}, \code{"pcoa"}, or \code{"pca"}).}

\item{color}{Optional character string naming the metadata column to color points by.}

\item{shape}{Optional character string naming the metadata column for point shapes.}

\item{ellipse}{Logical. If \code{TRUE} and \code{color} is provided, draws 95\% confidence ellipses around groups.}

\item{biplot}{Logical. If \code{TRUE} (and \code{method = "rpca"}), overlays top taxon loading vectors as arrows.}

\item{top_taxa}{Integer number of top driving taxa to annotate if \code{biplot = TRUE} (default: 5).}

\item{palette}{Palette name: \code{"tidybiome"} or \code{"nature"}.}
}
\value{
A \code{ggplot2::ggplot} object.
}
\description{
Generates elegant 2D ordination plots (RPCA, PCoA, PCA) with automatic variance
explained axis annotations, confidence ellipses, and optional taxon loading biplot vectors.
}
\examples{
counts <- matrix(c(100, 20, 5, 2, 5, 10, 80, 70, 1, 1, 50, 40), nrow = 3, byrow = TRUE,
                 dimnames = list(c("Tax1", "Tax2", "Tax3"), c("S1", "S2", "S3", "S4")))
sample_data <- data.frame(sample_id = c("S1", "S2", "S3", "S4"), group = c("A", "A", "B", "B"))
tb <- tidy_microbiome(counts, sample_data)
tb <- calc_ordination(tb, method = "rpca")
p <- plot_ordination(tb, method = "rpca", color = "group")
}
