% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_qc}
\alias{plot_qc}
\title{Plot Sample Quality Control Diagnostics}
\usage{
plot_qc(
  tb,
  x = "total_reads",
  y = "n_features",
  color_by = NULL,
  threshold_x = NULL,
  threshold_y = NULL,
  log_x = (x == "total_reads"),
  log_y = FALSE,
  palette = "tidybiome"
)
}
\arguments{
\item{tb}{A \code{tidy_microbiome} object.}

\item{x}{Metric for the x-axis: \code{"total_reads"}, \code{"n_features"}, \code{"sparsity"}, or \code{"top_taxon_share"}.
Defaults to \code{"total_reads"}.}

\item{y}{Metric for the y-axis. Defaults to \code{"n_features"}.}

\item{color_by}{Optional metadata column to color points by.}

\item{threshold_x}{Optional numeric threshold for vertical dashed cutoff line.}

\item{threshold_y}{Optional numeric threshold for horizontal dashed cutoff line.}

\item{log_x}{Logical; whether to log10-transform the x-axis (default: \code{TRUE} if \code{x == "total_reads"}).}

\item{log_y}{Logical; whether to log10-transform the y-axis (default: \code{FALSE}).}

\item{palette}{Palette name: \code{"tidybiome"} or \code{"nature"}.}
}
\value{
A \code{ggplot2::ggplot} object.
}
\description{
Generates publication-ready diagnostic scatter plots of sequencing library depth versus
observed taxonomic richness, with optional threshold lines, log scaling, and metadata grouping.
}
\examples{
data(gut_microbiome)
plot_qc(gut_microbiome, color_by = "treatment", threshold_x = 5000)
}
