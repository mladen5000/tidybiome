% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidy_microbiome.R
\name{tidy_microbiome}
\alias{tidy_microbiome}
\title{Create a tidy_microbiome Object}
\usage{
tidy_microbiome(
  counts,
  sample_data = NULL,
  tax_table = NULL,
  phy_tree = NULL,
  metadata = list()
)
}
\arguments{
\item{counts}{A numeric matrix of counts (taxa in rows, samples in columns).}

\item{sample_data}{Optional \code{data.frame} or \code{tbl_df} of sample metadata.
Must include a \code{sample_id} column or rownames matching \code{colnames(counts)}.}

\item{tax_table}{Optional \code{data.frame} or \code{tbl_df} of taxonomy annotations.
Must include a \code{taxon_id} column or rownames matching \code{rownames(counts)}.}

\item{phy_tree}{Optional phylogenetic tree (e.g. of class \code{phylo}).}

\item{metadata}{Optional list of arbitrary experiment metadata.}
}
\value{
An object of class \code{tidy_microbiome}, inheriting from \code{tbl_df}, \code{tbl}, and \code{data.frame}.
}
\description{
Creates a \code{tidy_microbiome} S3 object that inherits from \link[tibble:tbl_df]{tibble::tbl_df}.
The object behaves as a sample metadata tibble on the outside, while keeping
count matrices (assays), taxonomy annotations, phylogenetic trees, and
experiment metadata synchronized under the hood.
}
\examples{
counts <- matrix(c(10, 0, 5, 20), nrow = 2,
                 dimnames = list(c("ASV1", "ASV2"), c("S1", "S2")))
tb <- tidy_microbiome(counts)
tb
}
