% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transformations.R
\name{transform_abundance}
\alias{transform_abundance}
\title{Transform Abundance Data Using Compositional or Classical Normalization}
\usage{
transform_abundance(
  tb,
  method = c("rclr", "clr", "relabundance", "coverage", "log10", "pa"),
  assay = "counts",
  name = NULL,
  pseudocount = 1
)
}
\arguments{
\item{tb}{A \code{tidy_microbiome} object.}

\item{method}{Character string specifying the transformation method. Options:
\itemize{
\item \code{"rclr"}: Robust Centered Log-Ratio (Martino et al. 2019). Zero values remain zero;
positive values are centered by the geometric mean of observed (non-zero) taxa.
\item \code{"clr"}: Classic Centered Log-Ratio with pseudocount.
\item \code{"relabundance"}: Total Sum Scaling (TSS), scaling each sample to sum to 1.
\item \code{"coverage"}: Coverage-based standardization using Good-Turing sample coverage.
\item \code{"log10"}: Log10 transformation with pseudocount.
\item \code{"pa"}: Presence/Absence binary transformation (0 or 1).
}}

\item{assay}{Character string naming the source assay to transform. Defaults to \code{"counts"}.}

\item{name}{Optional character string naming the output assay. Defaults to \code{method}.}

\item{pseudocount}{Numeric pseudocount added for \code{"clr"} and \code{"log10"}. Defaults to 1.}
}
\value{
An updated \code{tidy_microbiome} object with the new assay added.
}
\description{
Applies modern compositional transformations (such as Robust CLR) or classic
normalizations (TSS, pseudocount CLR, presence/absence) to the abundance assay
of a \code{tidy_microbiome} object.
}
\examples{
counts <- matrix(c(100, 0, 50, 0, 20, 10, 0, 5), nrow = 2, byrow = TRUE,
                 dimnames = list(c("ASV1", "ASV2"), c("S1", "S2", "S3", "S4")))
tb <- tidy_microbiome(counts)
tb <- transform_abundance(tb, method = "rclr")
}
